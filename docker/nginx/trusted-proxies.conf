# Empty by design. The default deployment trusts no incoming Forwarded or
# X-Forwarded-For headers. The installer may replace this file only with
# explicit set_real_ip_from CIDRs for a known reverse proxy and real_ip_header.
